#include <bits/stdc++.h>
using namespace std;

int main(){
    int t, r, c;
    char grid[21][21];
    bool v[21][21];
    scanf("%d %d %d", &t, &r, &c);

    for(int i = 0; i<t; i++){
        memset(grid, '*', sizeof(grid));
        memset(v, false, sizeof(v));


        for(int i = 0; i<r; i++){
            for(int j = 0; j<c; j++)
        }
    }

}