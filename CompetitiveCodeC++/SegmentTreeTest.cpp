#include <bits/stdc++.h>
using namespace std;
int N, M;
int datArr[100001];
int minTree = [200001];
int GCDTree = [200001];






int main(){
    scanf("%d %d", &N, &M);
    for(int i = 0; i<N; i++){
        scanf("%d", &datArr[i]);
    }   

    char opp;
    int x, y;

    for(int i = 0; i<M; i++){
        scanf(" %c", &opp);
        if(opp == 'C'){

        } else if(opp == 'M'){
            
        } else if(opp == 'G'){
            
        } else {

        }
    }
}